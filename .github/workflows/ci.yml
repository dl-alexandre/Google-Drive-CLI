name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    env:
      GDRV_REQUIRE_CUSTOM_OAUTH: "1"
      GOTOOLCHAIN: auto
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: Download dependencies
        run: go mod download

      - name: Verify dependencies
        run: go mod verify

      - name: Run go vet
        run: go vet ./...

      - name: Run tests with flaky diagnostics (non-Windows)
        if: matrix.os != 'windows-latest'
        run: |
          set -o pipefail
          mkdir -p .artifacts/test
          attempt=1
          while [ $attempt -le 2 ]; do
            echo "Test attempt ${attempt}"
            if go test -json -v -race -coverprofile=coverage.out -covermode=atomic ./... | tee .artifacts/test/test-attempt-${attempt}.json; then
              exit 0
            fi
            attempt=$((attempt + 1))
          done
          exit 1

      - name: Run tests (Windows)
        if: matrix.os == 'windows-latest'
        run: go test -v ./...

      - name: Upload test diagnostics
        if: failure() && matrix.os != 'windows-latest'
        uses: actions/upload-artifact@v7
        with:
          name: gdrv-test-diagnostics-${{ matrix.os }}
          path: .artifacts/test/
          retention-days: 7

      - name: Upload coverage
        uses: codecov/codecov-action@v7
        if: matrix.os == 'ubuntu-latest'
        with:
          files: coverage.out
          fail_ci_if_error: false

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: test
    env:
      GDRV_REQUIRE_CUSTOM_OAUTH: "1"
      GOTOOLCHAIN: auto

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: Build optimized binaries
        run: make build-optimized build-all-optimized

      - name: Verify version metadata (optimized build)
        run: |
          ./bin/gdrv version
          VERSION="$(./bin/gdrv version)"
          test -n "$VERSION"
          test "$VERSION" != "dev"
          echo "✓ Version: $VERSION"
          echo "✓ Binary size: $(ls -lh bin/gdrv | awk '{print $5}')"

      - name: Generate checksums
        run: make checksums

      - name: Display binary sizes
        run: |
          echo "=== Optimized Binary Sizes ==="
          for binary in bin/gdrv-*; do
            if [ -f "$binary" ]; then
              size=$(ls -lh "$binary" | awk '{print $5}')
              platform=$(basename "$binary" | sed 's/gdrv-//')
              echo "  $platform: $size"
            fi
          done
          echo ""
          echo "Total size reduction: ~32% compared to debug builds"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: binaries
          path: |
            bin/
            bin/checksums.txt
          retention-days: 7

  lint:
    name: Lint
    runs-on: ubuntu-latest
    env:
      GDRV_REQUIRE_CUSTOM_OAUTH: "1"
      GOTOOLCHAIN: auto

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v9.3.0
        with:
          version: v2.12.2
          skip-cache: true
          args: --timeout=5m

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    env:
      GDRV_REQUIRE_CUSTOM_OAUTH: "1"
      GOTOOLCHAIN: auto

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: Run security scan
        run: make security
